import { PipemembrePipe } from './pipemembre.pipe';

describe('PipemembrePipe', () => {
  it('create an instance', () => {
    const pipe = new PipemembrePipe();
    expect(pipe).toBeTruthy();
  });
});
