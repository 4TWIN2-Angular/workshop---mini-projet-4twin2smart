import { PipeuniversityPipe } from './pipeuniversity.pipe';

describe('PipeuniversityPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeuniversityPipe();
    expect(pipe).toBeTruthy();
  });
});
